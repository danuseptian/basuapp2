import 'package:flutter/material.dart';

class StockistView extends StatelessWidget {
  const StockistView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
